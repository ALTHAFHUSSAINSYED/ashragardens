# ==============================================================================
# ASHRAGARDENS — ENVIRONMENT CONFIGURATION (VERCEL)
# ==============================================================================
# Because AshraGardens is built as a pure static website (Sprint 1-4 specs):
# - NO database connection strings are required (NO Supabase, NO PostgreSQL)
# - NO payment gateway API secrets are required (Direct PhonePe UPI QR is used)
# - NO authentication secrets are required
#
# All fields below are OPTIONAL and will default gracefully if omitted.
# ==============================================================================

# Production Site URL (Optional - defaults to https://ashragardens.com)
NEXT_PUBLIC_SITE_URL=https://ashragardens.com
